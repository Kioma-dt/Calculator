<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Calculator.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Calculator.View"
    Title="Calculator">

    <FlyoutItem Title="Calculator">
        <ShellContent
            Title="Calculator"
            ContentTemplate="{DataTemplate local:CalculatorPage}" />
    </FlyoutItem>
    <FlyoutItem Title="SinIntegral">
        <ShellContent
        Title="SinIntegral"
        ContentTemplate="{DataTemplate local:SinIntegralPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Hospital">
        <ShellContent
        Title="Hospital"
        ContentTemplate="{DataTemplate local:HospitalPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Rates">
        <ShellContent
        Title="Rates"
        ContentTemplate="{DataTemplate local:RatesPage}" />
    </FlyoutItem>

</Shell>
